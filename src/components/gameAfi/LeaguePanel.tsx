// League placeholder -- setup instructions land here once Head to Head is
// configured. No data/queries yet; this is intentionally just a stub per
// the current rollout plan.
export default function LeaguePanel() {
  return (
    <div className="rounded-2xl border border-card-border bg-card-bg p-5">
      <h3 className="mb-2 text-sm font-semibold text-text-primary">League</h3>
      <p className="text-sm text-text-muted">
        League setup instructions will show up here once Head to Head is configured. Send or accept a challenge on
        the Head to Head tab to get started.
      </p>
    </div>
  );
}
